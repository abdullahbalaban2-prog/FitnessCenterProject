@model FitnessCenterProject.Models.ContactMessage

@{
    ViewData["Title"] = "Mesaj Detayı";
}

<h1 class="section-title mb-3">Mesaj Detayı</h1>

<div class="card p-4">
    <p><strong>Gönderen:</strong> @Model.FullName</p>
    <p><strong>E-posta:</strong> @Model.Email</p>
    <p><strong>Telefon:</strong> @Model.Phone</p>
    <p><strong>Konu:</strong> @Model.Subject</p>
    <p><strong>Mesaj:</strong> @Model.Message</p>
    <p><strong>Tarih:</strong> @Model.CreatedAt.ToString("dd.MM.yyyy HH:mm")</p>

    <a asp-action="Index" class="btn btn-outline-secondary">Listeye Dön</a>
</div>
